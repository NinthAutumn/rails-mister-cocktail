<h1><%= @cocktail.name %></h1>

<ul>
  <% @cocktail.doses.each do |element| %>
    <li><%= element.description %></li>
  <% end %>
</ul>



<%= link_to "add dose", new_cocktail_dose_path(@cocktail), class: "btn btn-warning" %>
